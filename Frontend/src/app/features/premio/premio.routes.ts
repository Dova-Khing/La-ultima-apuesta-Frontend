import { Routes } from '@angular/router';
import { PremioListComponent } from './premio-list/premio-list.component';

export const PremioListComponentRoutes: Routes = [
    {
        path: '',
        component: PremioListComponent
    }
];
